/**
 * @param {Set} SetA
 * @param {Set} SetB
 * @returns create a set that contains those elements of set a that are also in set b.
 */
export declare const intersetSet: <T>(SetA: Set<T>, SetB: Set<T>) => Set<T>;
